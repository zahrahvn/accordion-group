import { AccordingBodyDirective } from './according-body.directive';

describe('AccordingBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordingBodyDirective();
    expect(directive).toBeTruthy();
  });
});
